name: KaaS-WebApp
summary: Creates a web app environment on KaaS
description: Creates a web app environment on KaaS
runner: Terraform
templatePath: main.tf
parameters:
  - id: "app_name"
    name: "App Name"
    required: true
    type: "string"